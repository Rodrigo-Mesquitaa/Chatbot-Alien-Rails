# Adicione suas próprias tarefas em arquivos colocados em lib / tasks que terminam em .rake,
# Por exemplo lib / tasks / capistrano.rake, e eles estarão automaticamente disponíveis para o Rake.

require_relative 'config/application'

Rails.application.load_tasks